apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: cloaknote
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: decoder4399/cloaknote-frontend:latest
          ports:
            - containerPort: 80
